# base64

Convert code to base64 and decrypt it back in a couple of clicks.

This works if someone has the foresight to convert their code to base64 and only then publish or send it to you.

# test

P.S.: base64 is not encryption.
